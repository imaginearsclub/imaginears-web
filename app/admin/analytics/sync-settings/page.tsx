import { SyncSettingsPage } from "@/components/admin/analytics/SyncSettingsPage";

export const metadata = {
  title: "Sync Settings | Analytics",
  description: "Configure automatic player data synchronization from Plan Analytics",
};

export default function Page() {
  return <SyncSettingsPage />;
}

